tree("Root")
	while
		AgentOn()
		fallback
			while
				NearPlayer(0.5)
				tree("AvoidPlayer")
			while
				IsMoving()
				tree("Run")
			ShouldMove(false)

tree("AvoidPlayer")
	sequence
		RandomPoint(5,0.5)
		while
			IsMoving()
			tree("Run")

tree("Run")
	sequence
		ShouldMove(true)
		PlayRunAnim()
tree("Pray")
	sequence
		PlayPrayAnim()

tree("Death")
	sequence
		PlayDeathAnim()
